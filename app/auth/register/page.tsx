import { RegisterForm } from "@/components/auth/register-form";
import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return <RegisterForm />;
};

export default page;
